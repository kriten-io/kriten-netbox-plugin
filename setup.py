from setuptools import find_packages, setup

setup(
    name='kriten-netbox',
    version='0.1.0',
    description='Configure Kriten and run fobs jobs from NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
